export enum ROLE {
  USER = 'user',
  ADMIN = 'admin',
}

export enum CONTACT_US_STATUS {
  PENDING = 'pending',
  RESPONDED = 'responded',
}

export enum PAYMENT_TYPE {
  HOURLY = 'hourly',
  FIXED = 'fixed',
}