export enum ROLE {
  USER = 'user',
  ADMIN = 'admin',
}

export enum CONTACT_US_STATUS {
  PENDING = 'pending',
  RESPONDED = 'responded',
}

export enum BUY_PLAN_STATUS {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}
